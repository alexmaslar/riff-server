# Rust
/target/
**/target/
Cargo.lock
!Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
*.xcuserstate

# Environment
.env
.env.local

# Config (may contain secrets)
config.yaml

# Database
*.db
*.db-shm
*.db-wal

# DCLAP model files (large binaries, fetched separately)
riff-core/models/dclap/*.onnx
riff-core/models/dclap/*.onnx.data
